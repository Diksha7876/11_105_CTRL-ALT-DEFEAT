spring.application.name=PaymentProcessing

# Local run defaults (override with DB_URL, DB_USER, DB_PASS, SERVER_PORT)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/PaymentProcessing?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:n3u3da!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always
server.port=${SERVER_PORT:8080}
